import 'package:flutter/material.dart';

class ResourcesBody extends StatelessWidget {
const ResourcesBody({ super.key });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Resources Page Coming Soon!'),
    );
  }
}
